PORT=3000
NODE_ENV=development
LOG_LEVEL=info

DB_HOST=localhost
DB_PORT=5432
DB_USER=taskflow
DB_PASSWORD=taskflow
DB_NAME=taskflow

JWT_SECRET=troque-este-valor-em-producao

# Usadas apenas por docker-compose.prod.yml + docker-compose.observability.yml
# (deploy.sh gera o .env real em produção a partir dos GitHub Secrets)
IMAGE_TAG=latest
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=troque-este-valor-em-producao
